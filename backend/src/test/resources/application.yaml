spring:
  ai:
    openai:
      api-key: test-dummy-key
  session:
    jdbc:
      initialize-schema: always

app:
  base-url: http://localhost
  backend-url: http://localhost
  mail-from: noreply@music-rec.local
  cors:
    allowed-origins: http://localhost
